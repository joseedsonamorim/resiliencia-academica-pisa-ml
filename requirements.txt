# ──────────────────────────────────────────────────────────────────────────────
# Dependências do projeto — Resiliência Criativa PISA 2022
# OPT-1: versões pinadas para reprodutibilidade computacional.
#
# Para instalar: pip install -r requirements.txt
# Ambiente Python recomendado: 3.10 — 3.12
# ──────────────────────────────────────────────────────────────────────────────

# Core data science
pandas>=2.0,<3.0
numpy>=1.24,<2.0
scipy>=1.11           # usado em clusterer.py (hierarchical), resilient_profile.py (Mann-Whitney)
statsmodels>=0.14     # CC-5: Benjamini-Hochberg FDR correction em resilient_profile.py

# Visualização
matplotlib>=3.7,<4.0
seaborn>=0.13,<1.0
Pillow>=10.0          # usado em clusterer.py para imagens/radar

# Configuração
pyyaml>=6.0,<7.0

# Machine Learning
scikit-learn>=1.3,<2.0
joblib>=1.3,<2.0

# Formatação de tabelas
tabulate>=0.9,<1.0

# Dashboard
streamlit>=1.28,<2.0

# Interpretabilidade (opcional — fallback para permutation_importance se ausente)
# shap>=0.44          # descomente para usar SHAP

# Aceleração de gradient boosting (opcional)
# xgboost>=2.0        # descomente para habilitar XGBoost
# lightgbm>=4.0       # descomente para habilitar LightGBM
# catboost>=1.2       # descomente para habilitar CatBoost
